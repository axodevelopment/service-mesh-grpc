apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: grpc-mesh-waypoint
  namespace: grpc-mesh-demo
  annotations:
    istio.io/waypoint-for: namespace
spec:
  gatewayClassName: istio-waypoint
  listeners:
  - name: mesh
    port: 15008
    protocol: HBONE